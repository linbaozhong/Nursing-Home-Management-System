// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tbldishes"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

const DishesTableName = "dishes"

var (
	dishesPool = pool.New[*Dishes](func() any {
		_obj := &Dishes{}
		return _obj
	})
)

func NewDishes() *Dishes {
	_obj := dishesPool.Get()
	return _obj
}

// MarshalJSON
func (p *Dishes) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(9 * 50)
	if p.Name != "" {
		write.WriteRaw("name", types.Marshal(p.Name))
	}
	if p.DelFlag != "" {
		write.WriteRaw("del_flag", types.Marshal(p.DelFlag))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.TypeId != 0 {
		write.WriteRaw("type_id", types.Marshal(p.TypeId))
	}
	if p.Price != 0.0 {
		write.WriteRaw("price", types.Marshal(p.Price))
	}
	if p.CreateId != 0 {
		write.WriteRaw("create_id", types.Marshal(p.CreateId))
	}
	if !p.CreateTime.IsZero() {
		write.WriteRaw("create_time", types.Marshal(p.CreateTime))
	}
	if p.UpdateId != 0 {
		write.WriteRaw("update_id", types.Marshal(p.UpdateId))
	}
	if !p.UpdateTime.IsZero() {
		write.WriteRaw("update_time", types.Marshal(p.UpdateTime))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *Dishes) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "name":
			p.Name = types.String(value.Str)
		case "del_flag":
			p.DelFlag = types.String(value.Str)
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "type_id":
			p.TypeId = types.BigInt(value.Uint())
		case "price":
			p.Price = types.Float64(value.Float())
		case "create_id":
			p.CreateId = types.BigInt(value.Uint())
		case "create_time":
			p.CreateTime = types.Time{Time: value.Time()}
		case "update_id":
			p.UpdateId = types.BigInt(value.Uint())
		case "update_time":
			p.UpdateTime = types.Time{Time: value.Time()}
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *Dishes) Free() {
	if p == nil {
		return
	}

	dishesPool.Put(p)
}

// Reset
func (p *Dishes) Reset() {
	p.Name = ""
	p.DelFlag = ""
	p.Id = 0
	p.TypeId = 0
	p.Price = 0
	p.CreateId = 0
	p.CreateTime = types.Time{}
	p.UpdateId = 0
	p.UpdateTime = types.Time{}

}

func (p *Dishes) TableName() string {
	return DishesTableName
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var dishesFieldToPtrFunc = map[string]func(*Dishes) any{
	tbldishes.Name.Name:       func(p *Dishes) any { return &p.Name },
	tbldishes.DelFlag.Name:    func(p *Dishes) any { return &p.DelFlag },
	tbldishes.Id.Name:         func(p *Dishes) any { return &p.Id },
	tbldishes.TypeId.Name:     func(p *Dishes) any { return &p.TypeId },
	tbldishes.Price.Name:      func(p *Dishes) any { return &p.Price },
	tbldishes.CreateId.Name:   func(p *Dishes) any { return &p.CreateId },
	tbldishes.CreateTime.Name: func(p *Dishes) any { return &p.CreateTime },
	tbldishes.UpdateId.Name:   func(p *Dishes) any { return &p.UpdateId },
	tbldishes.UpdateTime.Name: func(p *Dishes) any { return &p.UpdateTime },
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *Dishes) AssignPtr(args ...dialect.Field) []any {
	if len(args) == 0 {
		args = tbldishes.ReadableFields
	}

	_vals := make([]any, 0, len(args))
	for _, col := range args {
		if ptrFunc, ok := dishesFieldToPtrFunc[col.Name]; ok {
			_vals = append(_vals, ptrFunc(p))
		}
	}

	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *Dishes) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if ptrFunc, ok := dishesFieldToPtrFunc[col]; ok {
			_vals = append(_vals, ptrFunc(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *Dishes) Scan(rows *sql.Rows, args ...dialect.Field) ([]*Dishes, bool, error) {
	defer rows.Close()
	dishess := make([]*Dishes, 0)

	if len(args) == 0 {
		args = tbldishes.ReadableFields
	}

	for rows.Next() {
		_p := NewDishes()
		_vals := _p.AssignPtr(args...)
		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		dishess = append(dishess, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return dishess, len(dishess) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Dishes) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tbldishes.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var dishesFieldToValueFunc = map[dialect.Field]func(*Dishes) (any, bool){
	tbldishes.Name: func(p *Dishes) (any, bool) {
		return p.Name, p.Name == ""
	},
	tbldishes.DelFlag: func(p *Dishes) (any, bool) {
		return p.DelFlag, p.DelFlag == ""
	},
	tbldishes.Id: func(p *Dishes) (any, bool) {
		return p.Id, p.Id == 0
	},
	tbldishes.TypeId: func(p *Dishes) (any, bool) {
		return p.TypeId, p.TypeId == 0
	},
	tbldishes.Price: func(p *Dishes) (any, bool) {
		return p.Price, p.Price == 0.0
	},
	tbldishes.CreateId: func(p *Dishes) (any, bool) {
		return p.CreateId, p.CreateId == 0
	},
	tbldishes.CreateTime: func(p *Dishes) (any, bool) {
		return p.CreateTime, p.CreateTime.IsZero()
	},
	tbldishes.UpdateId: func(p *Dishes) (any, bool) {
		return p.UpdateId, p.UpdateId == 0
	},
	tbldishes.UpdateTime: func(p *Dishes) (any, bool) {
		return p.UpdateTime, p.UpdateTime.IsZero()
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Dishes) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tbldishes.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if valueFunc, exists := dishesFieldToValueFunc[arg]; exists {
			value, isZero := valueFunc(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *Dishes) AssignKeys() (dialect.Field, any) {
	return tbldishes.PrimaryKey, p.Id
}

func (p *Dishes) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
