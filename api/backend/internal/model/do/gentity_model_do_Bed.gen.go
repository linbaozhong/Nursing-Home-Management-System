// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tblbed"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

const BedTableName = "bed"

var (
	bedPool = pool.New[*Bed](func() any {
		_obj := &Bed{}
		return _obj
	})
)

func NewBed() *Bed {
	_obj := bedPool.Get()
	return _obj
}

// MarshalJSON
func (p *Bed) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(9 * 50)
	if p.Name != "" {
		write.WriteRaw("name", types.Marshal(p.Name))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.RoomId != 0 {
		write.WriteRaw("room_id", types.Marshal(p.RoomId))
	}
	if p.CreateId != 0 {
		write.WriteRaw("create_id", types.Marshal(p.CreateId))
	}
	if !p.CreateTime.IsZero() {
		write.WriteRaw("create_time", types.Marshal(p.CreateTime))
	}
	if p.UpdateId != 0 {
		write.WriteRaw("update_id", types.Marshal(p.UpdateId))
	}
	if !p.UpdateTime.IsZero() {
		write.WriteRaw("update_time", types.Marshal(p.UpdateTime))
	}
	if p.DelFlag != 0 {
		write.WriteRaw("del_flag", types.Marshal(p.DelFlag))
	}
	if p.BedFlag != 0 {
		write.WriteRaw("bed_flag", types.Marshal(p.BedFlag))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *Bed) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "name":
			p.Name = types.String(value.Str)
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "room_id":
			p.RoomId = types.BigInt(value.Uint())
		case "create_id":
			p.CreateId = types.BigInt(value.Uint())
		case "create_time":
			p.CreateTime = types.Time{Time: value.Time()}
		case "update_id":
			p.UpdateId = types.BigInt(value.Uint())
		case "update_time":
			p.UpdateTime = types.Time{Time: value.Time()}
		case "del_flag":
			p.DelFlag = types.Int8(value.Int())
		case "bed_flag":
			p.BedFlag = types.Int8(value.Int())
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *Bed) Free() {
	if p == nil {
		return
	}

	bedPool.Put(p)
}

// Reset
func (p *Bed) Reset() {
	p.Name = ""
	p.Id = 0
	p.RoomId = 0
	p.CreateId = 0
	p.CreateTime = types.Time{}
	p.UpdateId = 0
	p.UpdateTime = types.Time{}
	p.DelFlag = 0
	p.BedFlag = 0

}

func (p *Bed) TableName() string {
	return BedTableName
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var bedFieldToPtrFunc = map[string]func(*Bed) any{
	tblbed.Name.Name:       func(p *Bed) any { return &p.Name },
	tblbed.Id.Name:         func(p *Bed) any { return &p.Id },
	tblbed.RoomId.Name:     func(p *Bed) any { return &p.RoomId },
	tblbed.CreateId.Name:   func(p *Bed) any { return &p.CreateId },
	tblbed.CreateTime.Name: func(p *Bed) any { return &p.CreateTime },
	tblbed.UpdateId.Name:   func(p *Bed) any { return &p.UpdateId },
	tblbed.UpdateTime.Name: func(p *Bed) any { return &p.UpdateTime },
	tblbed.DelFlag.Name:    func(p *Bed) any { return &p.DelFlag },
	tblbed.BedFlag.Name:    func(p *Bed) any { return &p.BedFlag },
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *Bed) AssignPtr(args ...dialect.Field) []any {
	if len(args) == 0 {
		args = tblbed.ReadableFields
	}

	_vals := make([]any, 0, len(args))
	for _, col := range args {
		if ptrFunc, ok := bedFieldToPtrFunc[col.Name]; ok {
			_vals = append(_vals, ptrFunc(p))
		}
	}

	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *Bed) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if ptrFunc, ok := bedFieldToPtrFunc[col]; ok {
			_vals = append(_vals, ptrFunc(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *Bed) Scan(rows *sql.Rows, args ...dialect.Field) ([]*Bed, bool, error) {
	defer rows.Close()
	beds := make([]*Bed, 0)

	if len(args) == 0 {
		args = tblbed.ReadableFields
	}

	for rows.Next() {
		_p := NewBed()
		_vals := _p.AssignPtr(args...)
		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		beds = append(beds, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return beds, len(beds) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Bed) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tblbed.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var bedFieldToValueFunc = map[dialect.Field]func(*Bed) (any, bool){
	tblbed.Name: func(p *Bed) (any, bool) {
		return p.Name, p.Name == ""
	},
	tblbed.Id: func(p *Bed) (any, bool) {
		return p.Id, p.Id == 0
	},
	tblbed.RoomId: func(p *Bed) (any, bool) {
		return p.RoomId, p.RoomId == 0
	},
	tblbed.CreateId: func(p *Bed) (any, bool) {
		return p.CreateId, p.CreateId == 0
	},
	tblbed.CreateTime: func(p *Bed) (any, bool) {
		return p.CreateTime, p.CreateTime.IsZero()
	},
	tblbed.UpdateId: func(p *Bed) (any, bool) {
		return p.UpdateId, p.UpdateId == 0
	},
	tblbed.UpdateTime: func(p *Bed) (any, bool) {
		return p.UpdateTime, p.UpdateTime.IsZero()
	},
	tblbed.DelFlag: func(p *Bed) (any, bool) {
		return p.DelFlag, p.DelFlag == 0
	},
	tblbed.BedFlag: func(p *Bed) (any, bool) {
		return p.BedFlag, p.BedFlag == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Bed) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tblbed.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if valueFunc, exists := bedFieldToValueFunc[arg]; exists {
			value, isZero := valueFunc(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *Bed) AssignKeys() (dialect.Field, any) {
	return tblbed.PrimaryKey, p.Id
}

func (p *Bed) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
