# 员工端小程序 P0 六大模块 · 字段级细化规格

> 文档版本：v1.2
> 生成日期：2026-08-20
> 适用项目：`mp-b/admin`（微信小程序，uni-app-x，页面 `.uvue`、逻辑 UTS）
> 契约依据：`backend/internal/model/dto/*.go`（Go 后端为唯一事实源，字段一律用其 JSON snake_case 名）
> 配套方案：`mp-b/移动端复刻方案-员工端.md`
> 组件落地基线：`admin/components/README.md`
> 更新记录：
> - v1.1：新增 **§9 组件契约**（业务组件落位 `admin/components/` + 高阶模板 `ListPage`/`WizardForm`）
> - v1.2（2026-08-20）：适配**多租户**改造——
>   - §1 重写为「登录 · 权限 · 多租户」：新增 `/account/wxLogin` 与 `/tenant/*` 接口族；`LoginUserVO` 增加 `tenant_id / member_id / role_id / need_bind / tenants[]`；
>   - 新增页面：`pages/register`（创建企业）、`pages/switch-tenant`（选择企业）、`pages/join-tenant`（邀请码加入）；
>   - §6.4 紧急联系人增删改缺口已闭合（后端已补接口，详情页已实现联系人管理）；
>   - §7 接口适配表新增多租户条目；§9 组件契约标注实现状态（`ImageUploader` 改为 base64 + JSON 上传）。

---

## 0. 全局规范

### 0.1 接口约定
| 项 | 约定 |
|---|---|
| BaseURL | `https://<域名>/v1`（Go 路由全带 `/v1` 前缀，**非**老系统 `/api`） |
| 鉴权 | 免登录白名单：`/account/login`、`/account/wxLogin`、`/account/sendCode`、`/account/forget`、`/tenant/register`；其余全部需 `Authorization: Bearer <token>`（`lib.AuthMiddleware`） |
| 统一响应 | `Result { code:int, msg:string, data:object }`；分页数据在 `data` 内为 `PageResult { page_num, page_size, pages, size, total, list }` |
| 错误 | `code != 0` 时 toast `msg`；`code == 401` 清登录态跳登录页 |
| 多租户上下文 | token 格式 `userID[_tenantID[_memberID]]`，中间件解析后注入请求上下文；**旧 token（无租户段）视为平台租户成员**，鉴权通过但建议重新登录刷新 |
| 时间 | `time.Time` → 字符串 `2006-01-02 15:04:05`，移动端展示格式化 `MM-DD HH:mm` |
| 金额 | `types.Money`（`pay_amount`、`price`、`total_amount`、`really_amount` 等）→ JSON 数字（元），前端一律 `¥xx.xx`，禁止 `toFixed` 丢精度，建议分计算/BigInt |
| 分页 | **P0 列表全部取消分页**：请求 `page_num=1&page_size=200`，前端本地全量搜索/筛选（见 §7） |

### 0.2 通用组件
> 业务组件全部落位 `admin/components/`，props / 事件 / 状态字段级定义见 **§9 组件契约**。本节为速览，契约与实现状态以 §9 为准。

1. **WizardForm 向导高阶模板**（外出/来访/事故/点餐新增复用）：Step 指示器 + 上一步/下一步/提交，见 §9.2
2. **ElderSearch 长者搜索器**（外出/来访/事故/点餐/档案共用）
   - 触发：列表页顶部搜索框、或向导 Step1 的"选择老人"入口
   - 数据源：`GET /elderRecord/pageSearchElderByKey`（分页 200，本地过滤）
   - 弹层半屏：搜索框（`name`）+ 列表项 `[姓名] 身份证尾4位 / 电话 / 入住状态`
   - 返回选中对象 `{ id, name, id_num, sex, phone, address, check_flag }`，契约见 §9.3
3. **StaffSearch 员工搜索器**（事故-护工、送餐人员）
   - 数据源：`GET /reserve/pageSearchStaffByKey`（`{page_num,page_size,name?,phone?}` → `{id,name,phone}`）
   - 送餐人员另可用无参全量 `GET /nurseReserve/listNurseStaff` → `{id,name,phone}`
   - 契约见 §9.4
4. **DateTimePicker**：`picker mode="date"` + `mode="time"` 组合，输出 `yyyy-MM-dd HH:mm`；计划返回时间校验 > 外出时间，契约见 §9.5
5. **ImageUploader 图片上传**：`uni.chooseMedia`（最多 9 张）→ base64 → `POST /file/uploadImage`（JSON `{ module, file }`）→ 存返回 URL 字符串；支持预览/删除，契约见 §9.5
6. **StatusTag 状态标签**：色板见各模块与 §9.6
7. **DishesPicker 菜品选择器**（点餐）、**ContactQuickFill 联系人快捷填充**（外出）、**SectionCard 详情分区块**：见 §9.7~§9.9

> 注：P0 已实现组件见 §9.0 实现状态表；`ListPage`/`SectionCard` 尚未抽组件，列表页/详情分区块暂由页面内联。

---

## 1. 登录 · 权限 · 多租户（`pages/login`、`pages/register`、`pages/switch-tenant`、`pages/join-tenant`、`pages/mine`）

### 1.1 页面
| 页面 | 路径 | 说明 |
|---|---|---|
| 登录 | `pages/login/login` | **微信一键登录（主入口）** + 账号密码（备用）；已登录态自动进工作台 |
| 创建企业 | `pages/register/register` | 3 步向导：①企业信息 ②管理员账号 ③微信绑定确认；提交 `POST /tenant/register` 后直接进入工作台 |
| 选择企业 | `pages/switch-tenant/switch` | `GET /tenant/myTenants` 企业列表，点选 `POST /tenant/switchTenant` 切换；空态引导创建企业 |
| 加入企业 | `pages/join-tenant/join` | 输入邀请码 `POST /tenant/joinMember` |
| 个人中心 | `pages/mine/index` | 头像/姓名/手机号/修改密码/退出登录 |
| 修改密码 | `pages/mine/edit-pass` | `POST /account/edit` |

### 1.2 接口契约
| 接口 | 方法 | 鉴权 | 请求 | 响应 |
|---|---|---|---|---|
| `/account/login` | POST | 免 | `{ phone*, pass*, code? }` | `LoginUserVO` |
| `/account/wxLogin` | POST | 免 | `{ code* }` | `WxLoginVO { need_bind, token, user?, tenants[] }` |
| `/account/edit` | POST | 需 | `{ id*, old_pass*, new_pass* }` | `EmptyResp` |
| `/account/sendCode` | POST | 免 | `{ phone* }` | `EmptyResp`（备用） |
| `/account/logout` | GET | 需 | `-` | `EmptyResp` |
| `/tenant/register` | POST | 免 | `RegisterTenantQuery` | `LoginUserVO` |
| `/tenant/myTenants` | GET | 需 | `-` | `UserTenantListVO { tenants[]=TenantVO, current }` |
| `/tenant/switchTenant` | POST | 需 | `{ tenant_id* }` | `LoginUserVO`（重新签发 token） |
| `/tenant/joinMember` | POST | 需 | `{ invite_code* }` | `LoginUserVO` |
| `/tenant/inviteMember` | POST | 需（租户管理员） | `{ tenant_id*, phone*, role_id* }` | `MemberVO` |
| `/tenant/open` | POST | 需（平台管理员） | `{ id* }` | `EmptyResp` |
| `/tenant/lock` | POST | 需（平台管理员） | `{ id* }` | `EmptyResp` |

**LoginUserVO**（`/account/login`、`/tenant/register`、`/tenant/switchTenant` 均返回）：
```
{
  id: int64,             // 全局用户 id
  name, avator, phone, pass,
  tenant_id: int64,      // 当前租户编号
  member_id: int64,      // 当前成员编号
  role_id: int64,        // 当前角色编号
  need_bind: bool,       // true=需选择/绑定企业（前端据此分流）
  auth_id_list: int64[],
  auth_url_list: string[],
  tenants: TenantVO[],   // 已绑定企业列表（多企业时返回）
  token
}
```

**TenantVO**：`id, name, logo, contact_name, contact_phone, plan, status(int8: 0试用中/1正式/2锁定), trial_start, trial_end`
**WxLoginVO**：`need_bind, token, user(LoginUserVO|null), tenants(TenantVO[])`
**MemberVO**：`id, user_id, name, phone, role_id, status(int8: 0在职/1离职), auth_urls[]`

### 1.3 登录分流（need_bind 智能分流）
```
打开登录页 pages/login
 ├─ 已登录（token 非空）──> reLaunch 工作台 pages/index
 └─ 未登录：uni.login 取 code → POST /account/wxLogin（静默尝试）
      ├─ need_bind=false 且 user!=null ──> setLoginUser → 工作台（唯一企业自动登录）
      ├─ need_bind=true 且 tenants>0  ──> navigateTo pages/switch-tenant 选择企业
      └─ need_bind=true 且 tenants 空 ──> navigateTo pages/register 创建企业（新用户）
账号密码登录 /account/login 复用同一分流逻辑；登录页"没有企业？立即创建企业"→ register
```
- `pages/switch-tenant`：`GET /tenant/myTenants` 渲染企业卡片（名称/状态：试用中/正式/已锁定）→ 点击 `POST /tenant/switchTenant { tenant_id }` → `setLoginUser` → 工作台
- `pages/register`：3 步向导 → `POST /tenant/register`（携带 `wx_code` 完成微信绑定）→ 成功后 `setLoginUser` → 工作台；注册即开通 30 天免费试用（`TrialDays=30`），试用结束自动锁定
- `pages/join-tenant`：输入邀请码 → `POST /tenant/joinMember { invite_code }` → `setLoginUser` → 工作台

### 1.4 多租户数据落地（utils/auth.ts）
| Storage key | 内容 | 说明 |
|---|---|---|
| `token` | 后端签发 token | 请求头 `Authorization: Bearer <token>`（`utils/request.ts` 自动注入） |
| `login_user` | `LoginUser` 全量 | `getLoginUser()`；`setLoginUser(user)` 同时写 `token` 与 `current_tenant` |
| `current_tenant` | 当前租户 id | `getCurrentTenantId()` / `setCurrentTenantId()`；记住上次选择 |

- `clearAuth()`：三键全清（退出登录）
- 权限判断：`canAccess(module)`（`auth_url_list` 精确匹配）、`accessibleModules()` 过滤、`hasAuth(authId)`（`auth_id_list` 匹配）

### 1.5 权限映射（auth_url_list 驱动，已固化）
- 首页工作台功能入口 / 模块 **按 `auth_url_list` 显隐**：

| 模块 | 权限 url（后端 auth.url） | 前端 ModuleKey | 落地 |
|---|---|---|---|
| 长者档案 | `/people/old` | `elder` | TabBar / 工作台 |
| 外出登记 | `/check-in/leave` | `leave` | TabBar / 工作台 |
| 来访登记 | `/check-in/visit` | `visit` | 工作台入口 |
| 事故登记 | `/check-in/accident` | `accident` | 工作台入口 |
| 点餐 | `/food/order` | `order` | TabBar / 工作台 |

- 前端实现：`utils/auth.ts` 的 `canAccess(module)` + `accessibleModules()`；`pages/index` 工作台按权限渲染入口，头部展示「当前企业名」（从 `tenants` 匹配 `tenant_id`）；无权限时显示"暂无可用功能权限"。
- 角色约定：token 中角色标识 `tenant`（租户成员）/ `platform`（平台管理员）；平台管理员可调 `/tenant/open`、`/tenant/lock`。
- 注意：uni-app-x **原生 tabBar 无法登录后动态增删**，故 TabBar 4 项固定（档案/外出/点餐/我的），工作台 `pages/index` 为登录落地页（非 Tab），来访/事故作为工作台功能入口（`navigateTo`）按权限显隐。

---

## 2. 外出登记（`pages/leave`）

### 2.1 页面结构
| 页面 | 路径 | 说明 |
|---|---|---|
| 列表 | `pages/leave/list` | 全量加载 + 本地筛选 |
| 新增向导 | `pages/leave/edit` | 2 步：①选老人 ②陪同人+时间 |
| 详情/操作 | `pages/leave/detail` | 状态化按钮区 |

### 2.2 接口契约
| 接口 | 方法 | 请求 | 响应 |
|---|---|---|---|
| `/outward/pageOutwardByKey` | GET | `page_num,page_size,elder_name?,chaperone_type?,start_time?,end_time?` | 分页 `list[] = PageOutwardByKeyVO` |
| `/outward/addOutward` | POST | `AddOutwardQuery` | `EmptyResp` |
| `/outward/getOutwardById` | GET | `{ id }` | `GetOutwardByIDVO` |
| `/outward/editOutward` | POST | `EditOutwardQuery` | `EmptyResp` |
| `/outward/delayReturn` | POST | `{ id*, plan_return_date* }` | `EmptyResp` |
| `/outward/recordReturn` | POST | `{ id*, real_return_date* }` | `EmptyResp` |
| `/outward/deleteOutward` | POST | `{ id }` | `EmptyResp` |

**PageOutwardByKeyVO**：`id, elder_name, chaperone_name, chaperone_phone, chaperone_type, outward_date, plan_return_date, real_return_date`
**GetOutwardByIDVO**：`id, elder_name, chaperone_name, chaperone_phone, chaperone_type, outward_date, plan_return_date`
**AddOutwardQuery**（编辑=同字段+`id`）：`elder_id*, chaperone_name*, chaperone_phone*, chaperone_type*, outward_date*, plan_return_date*`

### 2.3 列表页
- **搜索区**：老人姓名输入框（本地过滤 `elder_name`）
- **筛选**：`chaperone_type` 分段器 `全部 / 护工 / 家属`；时间范围（可选，本地过滤 `outward_date`）
- **卡片**（一行一卡）：
  - 左侧：`老人姓名`（加粗）+ 状态 Tag
  - 右侧：`外出时间` → `计划返回时间`
  - 底部：`陪同人：xxx(家属/护工) · 138xxxx`，未返回且超时的显示红色"已超时"角标
- **状态派生逻辑**（后端不返回状态字段，前端本地推导）：

| 状态 | 判定 | Tag 色 |
|---|---|---|
| 外出中 | `real_return_date` 为空且未超时 | 蓝 |
| 已超时 | `real_return_date` 为空且 `now > plan_return_date` | 红 |
| 已返回 | `real_return_date` 非空 | 绿 |
- **空态**：无数据 → 插画 + "暂无外出记录" + 去登记按钮

### 2.4 新增向导（2 步）
| Step | 字段 | JSON | 类型 | 必填 | 组件 | 校验 |
|---|---|---|---|---|---|---|
| ①选老人 | 老人 | `elder_id` | int64 | ✅ | ElderSearch | 必选 |
| ②陪同人 | 姓名 | `chaperone_name` | string | ✅ | input | 必填 ≤20 字 |
| ②陪同人 | 电话 | `chaperone_phone` | string | ✅ | input type=number | 11 位手机号 |
| ②陪同人 | 类型 | `chaperone_type` | string | ✅ | radio | 枚举 `护工/家属` |
| ②时间 | 外出时间 | `outward_date` | datetime | ✅ | DateTimePicker | 必填 |
| ②时间 | 计划返回 | `plan_return_date` | datetime | ✅ | DateTimePicker | 必填，> outward_date |

- 陪同人**快捷填充**：`ContactQuickFill` 复用 `GET /elderRecord/pageSearchEmergencyContactByKey?elder_id=`，点击自动回填姓名/电话并置 `chaperone_type=家属`（见 §9.8）
- 提交：`addOutward` → 成功提示 → 返回列表刷新（新记录置顶）

### 2.5 详情/操作状态机
```
已返回(real_return_date!=空) ──> 仅"删除"可操作
外出中 ──> [延期返回] [登记返回] [删除]
```
- **延期返回**：弹窗 DateTimePicker（新 `plan_return_date`，默认当前时间+1h，必须晚于原计划）→ `delayReturn`
- **登记返回**：确认弹窗"确认该老人已返回？"（时间默认 now，可改）→ `recordReturn`
- **删除**：二次确认弹窗 → `deleteOutward`；删除后卡片消失

---

## 3. 来访登记（`pages/visitor`）

### 3.1 接口契约
| 接口 | 方法 | 请求 | 响应 |
|---|---|---|---|
| `/visit/pageVisitByKey` | GET | `page_num,page_size,elder_name?,visit_name?,visit_phone?,visit_flag?` | 分页 `list[] = PageVisitByKeyVO` |
| `/visit/addVisit` | POST | `AddVisitQuery` | `EmptyResp` |
| `/visit/getVisitById` | GET | `{ id }` | `GetVisitByIDVO` |
| `/visit/editVisit` | POST | `EditVisitQuery` | `EmptyResp` |
| `/visit/recordLeave` | POST | `{ id*, leave_date* }` | `EmptyResp` |
| `/visit/deleteVisit` | POST | `{ id }` | `EmptyResp` |

**PageVisitByKeyVO**：`id, elder_name, visit_name, visit_phone, relation, visit_date, leave_date, visit_num, visit_flag(int8: 0-在院/1-已离开)`
**GetVisitByIDVO**：`id, elder_name, visit_name, visit_phone, relation, visit_date, visit_num`（无 `leave_date/visit_flag`）
**AddVisitQuery**（编辑=同字段+`id`）：`elder_id*, name*, phone*, relation*, visit_date*, visit_num*`

### 3.2 列表页
- **搜索**：老人姓名（本地过滤 `elder_name`）
- **筛选**：`visit_flag` 分段器 `全部 / 在院(0) / 已离开(1)`——后端筛选直接传 `visit_flag`
- **卡片**：
  - 头部：`老人姓名` + Tag（在院=绿 / 已离开=灰）
  - 主体：`来访者：姓名(关系) · 138xxxx`；`人数 xN`
  - 底部：`来访时间`（已离开时追加 `离开时间`）
- **列表页快捷操作**：未离开卡片提供 `[登记离开]` 按钮（列表页直达，不必进详情）

### 3.3 新增向导（2 步）
| Step | 字段 | JSON | 类型 | 必填 | 组件 | 校验 |
|---|---|---|---|---|---|---|
| ① | 老人 | `elder_id` | int64 | ✅ | ElderSearch | 必选 |
| ② | 来访者姓名 | `name` | string | ✅ | input | 必填 ≤20 |
| ② | 来访者电话 | `phone` | string | ✅ | input | 11 位手机号 |
| ② | 与老人关系 | `relation` | string | ✅ | input | 必填 ≤10（儿子/女儿/护工） |
| ② | 来访时间 | `visit_date` | datetime | ✅ | DateTimePicker | 默认 now |
| ② | 来访人数 | `visit_num` | int | ✅ | stepper | 1~20，默认 1 |

### 3.4 详情/操作状态机
```
在院(visit_flag=0) ──> [登记离开] [编辑] [删除]
已离开(visit_flag=1) ──> [删除]（编辑禁用）
```
- **登记离开**：确认弹窗（`leave_date` 默认 now）→ `recordLeave` → Tag 变灰
- **编辑**：复用新增向导（`getVisitById` 回填，`elder_id` 锁定不可改）→ `editVisit`
- **删除**：二次确认 → `deleteVisit`

---

## 4. 事故登记（`pages/accident`）

### 4.1 接口契约
| 接口 | 方法 | 请求 | 响应 |
|---|---|---|---|
| `/accident/pageAccidentByKey` | GET | `page_num,page_size,elder_name?,staff_name?` | 分页 `list[] = PageAccidentByKeyVO` |
| `/accident/addAccident` | POST | `AddAccidentQuery` | `EmptyResp` |
| `/accident/getAccidentById` | GET | `{ id }` | `GetAccidentByIDVO` |
| `/accident/editAccident` | POST | `EditAccidentQuery` | `EmptyResp` |
| `/accident/deleteAccident` | POST | `{ id }` | `EmptyResp` |

**PageAccidentByKeyVO**：`id, elder_name, staff_name, occur_date`
**GetAccidentByIDVO**：`id, elder_name, staff_id, occur_date, description, picture`
**AddAccidentQuery**（编辑=同字段+`id`）：`elder_id*, staff_id*, occur_date*, description*, picture*`

### 4.2 列表页
- **搜索**：老人姓名（本地过滤 `elder_name`）
- **卡片**：`老人姓名` + `护工：xxx` + `发生时间`；红"事故"Tag；**列表页可本地筛 `staff_name`**
- 默认按 `occur_date` 倒序

### 4.3 新增向导（3 步，全量字段见 AccidentQuery）
| Step | 字段 | JSON | 类型 | 必填 | 组件 | 校验 |
|---|---|---|---|---|---|---|
| ① | 老人 | `elder_id` | int64 | ✅ | ElderSearch | 必选 |
| ① | 护工 | `staff_id` | int64 | ✅ | StaffSearch（`pageSearchStaffByKey`） | 必选 |
| ② | 发生时间 | `occur_date` | datetime | ✅ | DateTimePicker | 默认 now，不允许未来 |
| ③ | 事故描述 | `description` | textarea | ✅ | textarea | 必填 ≤500 字 |
| ③ | 事故图片 | `picture` | string(URL) | ✅ | ImageUploader | **至少 1 张**；`uni.chooseMedia` → base64 → `/file/uploadImage` 返回 URL 后提交 |

> ⚠️ 后端 `picture` 为**单个字符串**（`valid:"required"`）：多图按"逗号拼接"存储，展示时按 `,` 拆分；前端 `ImageUploader` 的 `modelValue` 即逗号分隔的 URL 串（见 §9.5）。

### 4.4 详情/操作
- 详情：老人/护工/发生时间 + 图片九宫格预览（`previewImage`）+ 描述全文
- 操作：`[编辑]`（回填向导，老人可改）、`[删除]`（二次确认）
- 无状态机（记录型模块）；编辑后列表同步刷新

---

## 5. 点餐（`pages/order`）

### 5.1 接口契约
| 接口 | 方法 | 请求 | 响应 |
|---|---|---|---|
| `/order/pageOrderByKey` | GET | `page_num,page_size,elder_name?,elder_phone?` | 分页 `list[] = PageOrderByKeyVO` |
| `/order/addOrder` | POST | `AddOrderQuery` | `EmptyResp` |
| `/order/getOrderById` | GET | `{ id }` | `GetOrderByIDVO` |
| `/order/sendOrder` | POST | `{ id*, staff_id*, deliver_dishes_date* }` | `EmptyResp` |
| `/dishes/pageDishesByKey` | GET | `page_num,page_size,type_id?,dishes_name?` | 分页 `list[] = PageDishesByKeyVO` |
| `/nurseReserve/listNurseStaff` | GET | 无参 | `[] = { id, name, phone }` |

**PageOrderByKeyVO**：`id, elder_name, elder_phone, dine_date, dine_type, staff_name, deliver_dishes_date, pay_amount(Money), order_flag(string: "是"=已送餐/"否"=待送餐)`
**GetOrderByIDVO**：`elder_name, elder_phone, dine_date, dine_type, staff_name, deliver_dishes_date, order_dishes_vo_list[]`
**OrderDishesVO**：`dishes_name, dishes_price(Money), order_num, set_flag, total_amount(Money), really_amount(Money)`
**AddOrderQuery**：`elder_id*, dine_date*, dine_type*, order_dishes_list*[] = { dishes_id*, order_num* }`
**PageDishesByKeyVO**：`id, type_name, dishes_name, price(Money)`

### 5.2 列表页
- **搜索**：老人姓名 / 电话（本地过滤）
- **筛选**：`order_flag` 分段器 `全部 / 待送餐 / 已送餐`（本地过滤；`order_flag="是"→已送餐`）
- **卡片**：
  - 头部：`老人姓名` + Tag（待送餐=橙 / 已送餐=绿）
  - 主体：`堂食/送餐 · MM-DD HH:mm 就餐`；`已点 x 道 · ¥xxx.xx`
  - 底部：已送餐时显示 `送餐人：xxx`；**待送餐 + 送餐方式=送餐**时显示 `[去送餐]` 按钮
- `dine_type` 枚举：`送餐/堂食`；`堂食`订单**不出现送餐按钮**

### 5.3 新增向导（2 步）
| Step | 字段 | JSON | 类型 | 必填 | 组件 | 校验 |
|---|---|---|---|---|---|---|
| ① | 老人 | `elder_id` | int64 | ✅ | ElderSearch | 必选 |
| ① | 就餐时间 | `dine_date` | datetime | ✅ | DateTimePicker | 必填 |
| ① | 就餐方式 | `dine_type` | string | ✅ | radio | `送餐/堂食` |
| ② | 菜品列表 | `order_dishes_list` | array | ✅ | DishesPicker | ≥1 项 |
| ② | 菜品项 | `dishes_id` | int64 | ✅ | - | 必填 |
| ② | 份数 | `order_num` | int | ✅ | stepper | 1~99 |

- **菜品选择器**：全量加载 `/dishes/pageDishesByKey` → 左分类右列表（`type_name` 分组），每项 `菜品名 ¥price` + 步进器；底部实时合计 `Σ(price×num)`（本地计算展示，提交金额以服务端 `pay_amount` 为准）
- 提交 `addOrder` → 成功提示 → 返回列表

### 5.4 送餐操作（状态机）
```
待送餐 + 送餐 ──> [去送餐] ──> 选送餐人员(listNurseStaff) + 送餐时间(默认now)
                ──> sendOrder ──> 已送餐(卡片变绿)
待送餐 + 堂食 / 已送餐：无送餐入口
```
- **送餐人员选择**：`GET /nurseReserve/listNurseStaff`（无参全量）弹层列表 `{id,name,phone}` → 提交 `sendOrder`（`staff_id`、`deliver_dishes_date`）
- 点餐模块无删除/编辑操作（P0 范围外，如需删除需后端新增 `deleteOrder`）

---

## 6. 长者档案（`pages/elder`）

### 6.1 接口契约
| 接口 | 方法 | 请求 | 响应 |
|---|---|---|---|
| `/elderRecord/pageElderRecordByKey` | GET | `page_num,page_size,elder_name?,id_num?,elder_sex?` | 分页 `list[] = PageElderByKeyVO` |
| `/elderRecord/getElderRecordById` | GET | `{ id }` | `GetElderRecordByIDVO` |
| `/elderRecord/addElderRecord` | POST | `AddElderRecordQuery` | `EmptyResp` |
| `/elderRecord/editElderRecord` | POST | `EditElderRecordQuery` | `EmptyResp` |
| `/elderRecord/deleteElderRecord` | POST | `{ id }` | `EmptyResp` |
| `/elderRecord/pageSearchEmergencyContactByKey` | GET | `page_num,page_size,elder_id*,key?` | 分页 `list[] = PageSearchEmergencyContactByKeyVO` |
| `/elderRecord/addEmergencyContact` | POST | `{ elder_id*, name*, phone*, relation? }` | `EmptyResp` |
| `/elderRecord/editEmergencyContact` | POST | `{ id*, name?, phone?, relation? }` | `EmptyResp` |
| `/elderRecord/deleteEmergencyContact` | POST | `{ id }` | `EmptyResp` |
| `/elderRecord/pageLabelByKey` | GET | `page_num,page_size,key?` | 分页 `list[] = ListLabelVO` |
| `/elderRecord/editElderLabel` | POST | `{ elder_id*, label_id_list* }` | `EmptyResp` |

**PageElderByKeyVO**：`id, bed_name, name, id_num, age, sex, phone, address, check_flag`
**GetElderRecordByIDVO**：`name, id_num, age, sex, phone, address, elder_emergency_contact_by_id_vo_list[], elder_nurse_grade_by_id_vo, elder_catering_set_by_id_vo, elder_bed_by_id_vo`
**紧急联系人**：`id, elder_id, name, phone, relation, remark`
**标签**：`ListLabelVO { id, name, label_item_list[] = { id, type_id, name, color } }`；**分类级 CheckboxGroup，选中项提交 `label_id_list`**
**Add/EditElderRecordQuery**：`id, name*, id_num*, sex*, age*, phone*, address*, nurse_level?, check_flag?`

### 6.2 列表页
- **搜索**：姓名 / 身份证号（本地过滤 `name`、`id_num`）
- **筛选**：性别分段器 `全部/男/女`（本地过滤 `sex`）
- **卡片**：`姓名`（加粗）+ `sex/age` 徽标 + 入住状态 Tag（`check_flag` 文案直出，色板：入住=绿 / 未入住=灰，具体枚举值**待后端核对**）；副行 `床号 · 电话`；搜索命中身份证时高亮
- FAB 悬浮 `[+ 新增长者]`

### 6.3 新增/编辑表单
| 字段 | JSON | 类型 | 必填 | 组件 | 校验 |
|---|---|---|---|---|---|
| 姓名 | `name` | string | ✅ | input | 必填 ≤20 |
| 身份证号 | `id_num` | string | ✅ | input | 18 位身份证正则 |
| 性别 | `sex` | string | ✅ | radio | `男/女`（与 web 一致，**待确认是否为 0/1**） |
| 年龄 | `age` | int | ✅ | input number | 0~120 |
| 电话 | `phone` | string | ✅ | input | 11 位手机号 |
| 地址 | `address` | string | ✅ | input | 必填 |
| 护理等级 | `nurse_level` | string | 否 | picker | 待后端字典 |
| 入住状态 | `check_flag` | string | 否 | picker | 待后端字典 |

- 编辑回填 `getElderRecordById`
- 删除：列表长按 → 二次确认（有 `elder_bed_by_id_vo` 在住记录时提示风险）

### 6.4 详情页
- 分区块：基本信息 → 床位（`elder_bed_by_id_vo.bed_name/room_type/month_price`）→ 护理等级（`elder_nurse_grade_by_id_vo.name/month_price`）→ 紧急联系人（列表 + **管理入口**）→ 标签（`pageLabelByKey`+`editElderLabel`）
- 操作：`[编辑]`（跳表单）、`[编辑标签]`（弹层勾选标签 → `editElderLabel`）
- **紧急联系人管理**（✅ 已实现）：详情页"管理"弹层支持 `addEmergencyContact` / `editEmergencyContact` / `deleteEmergencyContact`；外出陪同人快捷填充复用其查询（§2.4）
- ⚠️ 剩余缺口：标签编辑（`editElderLabel`）与护理等级/床位信息以展示为主；紧急联系人 CRUD 已闭环

---

## 7. 接口适配表（移动端改造点汇总）

| # | 后端接口 | 移动端改造 | 状态 |
|---|---|---|---|
| 1 | 全部 `pageXxxByKey` | 传 `page_num=1&page_size=200`，本地全量搜索/筛选；后端 `page_size` 上限若 <200 需放开或确认 | ⚠️ 需后端确认上限 |
| 2 | `/outward/pageOutwardByKey` | 返回无状态字段 → 前端按 `real_return_date`/`plan_return_date` 推导"外出中/已超时/已返回" | 已确认 |
| 3 | `/visit/pageVisitByKey` | `visit_flag` 可后端过滤，其余本地 | 已确认 |
| 4 | `/order/pageOrderByKey` | `order_flag="是/否"` → 前端映射"已送餐/待送餐" | 已确认 |
| 5 | `/dishes/pageDishesByKey` | **路由在 `/dishes` 下**（非老系统 `/order`），移动端需按此调用 | 已确认 |
| 6 | `/nurseReserve/listNurseStaff` | 复用为送餐人员选择（无参全量） | 已确认 |
| 7 | `/elderRecord/pageSearchEmergencyContactByKey` | 外出向导填充陪同人、长者详情联系人列表；**增删改已补**（`add/edit/deleteEmergencyContact`） | 已确认 |
| 8 | `/reserve/pageSearchStaffByKey` | 复用为事故-护工选择（`{page_num,page_size,name?,phone?}` → `{id,name,phone}`） | 已确认 |
| 9 | `/file/uploadImage` | 事故图片上传：**`uni.chooseMedia` → base64 → JSON `{ module, file }`**，返回 `{ id, url }` 写入 `picture`（非 FormData） | 已确认 |
| 10 | `/account/login` | 登录后持久化 `token` + `auth_id_list` + `tenants`，做模块白名单与多租户分流 | 已确认 |
| 11 | `/account/wxLogin` | **微信一键登录（主入口）**：`uni.login` 取 code → `need_bind` 分流（自动登录/选企业/创建企业） | 已确认 |
| 12 | `/tenant/register` | 企业自助注册（免登录），成功后写登录态直接进工作台 | 已确认 |
| 13 | `/tenant/myTenants` + `/tenant/switchTenant` | 选择企业页：列表 + 点选切换（重签 token） | 已确认 |
| 14 | `/tenant/joinMember` | 加入企业页：邀请码加入 | 已确认 |
| 15 | `/tenant/inviteMember` | 成员邀请（租户管理员），移动端入口待 P1 | ⚠️ 按范围裁剪 |
| 16 | 订单删除/编辑 | Go 后端无 `deleteOrder`/`editOrder`，P0 点餐无删除入口 | ⚠️ 按范围裁剪 |
| 17 | `check_flag`/`sex`/`nurse_level` 枚举值 | DTO 为 string，取值字典待后端核对（web 端按文本直出） | ⚠️ 需核对 |

---

## 8. 后续细化路线（建议）
1. **对齐后端缺口**：§7 中 ⚠️（`page_size` 上限、字典枚举、订单删除、成员邀请入口）需先与后端负责人确认。
2. **组件补齐**：`wizard-form/WizardForm.uvue` 已由 4 个新增向导引用但组件文件尚缺，需落地；`ListPage`/`SectionCard` 暂由页面内联（见 §9 实现状态）。
3. **页面骨架**：`pages.json` TabBar（档案/外出/点餐/我的）+ 路由注册（含 register/switch-tenant/join-tenant）✅ 已完成。
4. **P1 多租户增强**：「我的」页挂企业管理入口（切换/创建/加入企业）、成员邀请（`inviteMember`）。
5. **P1/P2 及家属端**：后续迭代。

---

## 9. 组件契约

> 落地方式：**业务组件全部新建在 `admin/components/` 目录**（uni-app-x，`.uvue`），与第三方 `uni_modules/` 隔离、便于随业务演进；
> 依赖的 uni 官方组件（`uni-nav-bar-x`、`uni-number-box-x`、`uni-badge-view`、`uni-time-format`）从 `uni_modules/` 直接引用，不重复封装。
> 实现状态以 `admin/components/README.md` 与本节「实现状态」列为准。

### 9.0 组件总览

| 组件 | 落位 | 用途 | 实现状态 |
|---|---|---|---|
| `WizardForm` | `components/wizard-form/` | 多步向导模板：Step 指示器 + 上一步/下一步/提交 | ⚠️ 4 个新增向导已引用，组件文件待补齐 |
| `ElderSearch` | `components/elder-search/` | 长者搜索器（弹层半屏，外出/来访/事故/点餐/长者档案复用） | ✅ 已实现 |
| `StaffSearch` | `components/staff-search/` | 员工搜索器（事故护工、送餐人员） | ✅ 已实现 |
| `DateTimePicker` | `components/date-time-picker/` | 日期+时间组合选择，输出 `yyyy-MM-dd HH:mm` | ✅ 已实现 |
| `ImageUploader` | `components/image-uploader/` | 图片选择上传（base64 → `/file/uploadImage`），预览/删除 | ✅ 已实现 |
| `StatusTag` | `components/status-tag/` | 状态标签（色板见 §9.6） | ✅ 已实现 |
| `DishesPicker` | `components/dishes-picker/` | 点餐菜品选择器（分类+列表+步进器+实时合计） | ✅ 已实现 |
| `ContactQuickFill` | `components/contact-quick-fill/` | 紧急联系人快捷填充（外出陪同人） | ✅ 已实现 |
| `ListPage` | `components/list-page/`（规划） | 列表页高阶模板：搜索/筛选/卡片/空态/刷新/分页壳 | ⬜ 待实现（暂页面内联） |
| `SectionCard` | `components/section-card/`（规划） | 详情页分区块容器（标题 + 内容 + 分隔线） | ⬜ 待实现（暂页面内联） |

> 页面与组件的引用关系：`WizardForm` 被外出/来访/事故/点餐 4 个新增向导复用；`ElderSearch`/`StatusTag` 为跨模块通用；`ListPage` 待抽（列表页骨架暂由各页内联）。

---

### 9.1 高阶模板 ListPage（列表页）——⬜ 待实现

> 规划抽象 5 个列表页（外出/来访/事故/点餐/长者档案）的公共骨架：**全量加载 + 本地筛选 + 卡片渲染 + 空态 + 下拉刷新 + 上拉加载(可选)**。当前各列表页内联实现，抽组件时以下列契约为准。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `title` | string | - | 导航标题（透传 `uni-nav-bar-x`） |
| `loading` | boolean | false | 数据加载中（骨架屏） |
| `list` | any[] | [] | 渲染数据（父页本地过滤后传入） |
| `emptyText` | string | `暂无数据` | 空态文案 |
| `emptyActionText` | string | - | 空态按钮文案（如"去登记"），空则不显示 |
| `showSearch` | boolean | true | 是否显示顶部搜索框 |
| `searchPlaceholder` | string | `搜索` | 搜索框占位 |
| `filterSlot` | boolean | false | 是否启用筛选区插槽 |
| `pullRefresh` | boolean | true | 启用下拉刷新 |
| `loadMore` | boolean | false | 启用上拉加载 |

**Slots**

| 插槽 | 说明 |
|---|---|
| `search-bar` | 自定义搜索区（默认渲染内置搜索框） |
| `filter` | 筛选区（`showFilter` 时生效，放分段器/时间范围等） |
| `card` | 卡片区（必填，绑定 `item` 逐项渲染） |
| `empty-action` | 空态按钮自定义 |

**Events**

| 事件 | 参数 | 说明 |
|---|---|---|
| `@search` | `(keyword: string)` | 搜索框输入（父页做本地过滤） |
| `@refresh` | - | 下拉刷新触发（父页重新拉全量） |
| `@loadmore` | - | 上拉加载触发 |
| `@emptyAction` | - | 空态按钮点击（跳新增页） |

**内部状态/行为**

- 本地维护 `scroll`、`refreshing`、`isEnd` 状态；
- 无数据时渲染 `emptyText` + 可选 `emptyAction`；
- 对外暴露 `beginRefresh()` 供父页下拉后复位。

**落地清单**：外出列表、来访列表、事故列表、点餐列表、长者档案列表。

---

### 9.2 高阶模板 WizardForm（多步向导）——⚠️ 已引用待补齐

> 抽象外出/来访/事故/点餐 4 个新增向导：Step 指示器 + 每步表单校验 + 上一步/下一步/提交。`pages/leave|visitor|accident|order/edit.uvue` 已按此契约 import `components/wizard-form/WizardForm.uvue`，**组件文件待补齐**。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `steps` | string[] | [] | 步骤标题数组（如 `["选老人","陪同人"]`） |
| `current` | number | 0 | 当前步骤索引（`v-model`） |
| `submitText` | string | `提交` | 末步提交按钮文案 |
| `disabled` | boolean | false | 提交禁用（表单未过校验时置 true） |

**Slots**

| 插槽 | 说明 |
|---|---|
| `step-N`（如 `step-0`） | 每一步的表单内容 |
| `footer` | 底部按钮区自定义（默认渲染 上一步/下一步/提交） |

**Events**

| 事件 | 参数 | 说明 |
|---|---|---|
| `@change` | `(index: number)` | 步骤切换 |
| `@submit` | - | 末步提交点击（父页执行对应 `addXxx`） |
| `@prev` / `@next` | - | 上一步 / 下一步（父页校验当前步） |

**内部状态/行为**

- 头部 Step 指示器（当前高亮，已完成打勾）；
- 非末步渲染 `下一步`，首步隐藏 `上一步`，末步渲染 `提交`；
- 每步切换前由父页 `@prev/@next` 校验该步字段，未过则拦截。

**落地清单**：外出新增、来访新增、事故新增、点餐新增。

---

### 9.3 ElderSearch 长者搜索器（✅ 已实现）

> 数据源 `GET /elderRecord/pageSearchElderByKey`（`page_num=1&page_size=200` 全量本地过滤）。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `visible` | boolean | false | 弹层显隐（`v-model`） |
| `placeholder` | string | `输入老人姓名` | 搜索框占位 |
| `excludeIds` | int64[] | [] | 需要置灰禁选的 id（编辑态锁定场景） |

**Events**

| 事件 | 参数 | 说明 |
|---|---|---|
| `@select` | `(elder: ElderItem)` | 选中老人回调 |
| `@close` | - | 关闭弹层 |

**返回对象 `ElderItem`**

```
{ id:int64, name:string, id_num:string, sex:string, phone:string, address:string, check_flag:string }
```

**渲染项**：`[姓名] 身份证尾4位 / 电话 / 入住状态Tag`。
**交互**：半屏弹层 + 顶部搜索框（本地过滤 `name`），点击项回调 `select` 并关闭。

**落地清单**：外出/来访/事故/点餐新增 Step1，长者档案。

---

### 9.4 StaffSearch 员工搜索器（✅ 已实现）

> 数据源 `GET /reserve/pageSearchStaffByKey`（`{page_num,page_size,name?,phone?}` → `{id,name,phone}`）；送餐另可 `GET /nurseReserve/listNurseStaff` 无参全量。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `visible` | boolean | false | 弹层显隐（`v-model`） |
| `source` | `"reserve"` \| `"nurse"` | `reserve` | 数据源：`reserve`=pageSearchStaffByKey，`nurse`=listNurseStaff |

**Events**

| 事件 | 参数 | 说明 |
|---|---|---|
| `@select` | `(staff: { id:int64, name:string, phone:string })` | 选中员工 |
| `@close` | - | 关闭 |

**落地清单**：事故-护工（`source="reserve"`）、点餐-送餐人员（`source="nurse"`）。

---

### 9.5 表单类：DateTimePicker / ImageUploader（✅ 已实现）

**DateTimePicker**：`picker mode="date"` + `mode="time"` 组合。

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `modelValue` | string | - | 输出值 `yyyy-MM-dd HH:mm`（`v-model`） |
| `placeholder` | string | `请选择时间` | 占位 |
| `disabled` | boolean | false | 禁用 |
| `min` / `max` | string | - | 允许范围（`yyyy-MM-dd HH:mm`） |

**Events**：`@change (value: string)`。

**ImageUploader**：`uni.chooseMedia`（最多 9 张）→ `getFileSystemManager().readFileSync(filePath, 'base64')` → `POST /file/uploadImage`（JSON `{ module, file: 'data:image/jpeg;base64,...' }`）→ 返回 `{ id, url }` 存 URL。

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `modelValue` | string | - | 图片 URL（多张用 `,` 拼接，`v-model`） |
| `max` | number | 9 | 最多张数 |
| `module` | string | `accident` | 上传归属模块（透传 `module` 字段） |

**Events**：`@update:modelValue (value: string)` / `@change (value: string)`。

**落地清单**：外出/来访/事故/点餐的时间字段；事故图片。

---

### 9.6 StatusTag 状态标签（✅ 已实现）

> 纯展示，按 `type` 映射色板，自绘圆角标签。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `text` | string | - | 标签文案 |
| `type` | `primary`/`success`/`warning`/`danger`/`default` | `default` | 色板 |

**色板约定**（对应 §2/§3/§5/§6 状态）

| 场景 | 文案 | type | 色值 |
|---|---|---|---|
| 外出-外出中 | 外出中 | `primary` | 蓝 `#007aff` |
| 外出-已超时 | 已超时 | `danger` | 红 `#dd524d` |
| 外出-已返回 | 已返回 | `success` | 绿 `#4cd964` |
| 来访-在院 | 在院 | `success` | 绿 |
| 来访-已离开 | 已离开 | `default` | 灰 |
| 事故 | 事故 | `danger` | 红 |
| 点餐-待送餐 | 待送餐 | `warning` | 橙 `#f0ad4e` |
| 点餐-已送餐 | 已送餐 | `success` | 绿 |
| 长者-入住 | 入住中 | `success` | 绿 |
| 长者-未入住 | 未入住 | `default` | 灰 |

---

### 9.7 DishesPicker 菜品选择器（✅ 已实现）

> 全量加载 `/dishes/pageDishesByKey` → 左分类右列表（`type_name` 分组），每项菜品 + 步进器；底部实时合计。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `modelValue` | `{dishes_id:int64, order_num:int}[]` | [] | 已选菜品（`v-model`） |
| `max` | number | 50 | 菜品总数上限 |

**Events**：`@change (list: OrderDishes[])`。

**内部行为**：本地维护分类索引 + 选中 Map；合计 `Σ(price×num)` 实时展示（提交金额以服务端 `pay_amount` 为准）。

---

### 9.8 ContactQuickFill 紧急联系人快捷填充（✅ 已实现）

> 复用 `GET /elderRecord/pageSearchEmergencyContactByKey?elder_id=`，列出该老人紧急联系人，点击回填陪同人（姓名/电话 + `chaperone_type=家属`）。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `visible` | boolean | false | 弹层显隐（`v-model`） |
| `elderId` | int64 | - | 目标老人（必传） |

**Events**：`@select (contact: { name:string, phone:string })`。

---

### 9.9 SectionCard 详情分区块（⬜ 待实现）

> 长者档案/各详情页分区块暂由页面内联，抽组件时以下列契约为准。

**Props**

| 属性 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `title` | string | - | 区块标题 |
| `actionText` | string | - | 右上角操作文字（如"编辑标签"），空则不显示 |

**Events**：`@action` - 操作点击。

**Slots**：`default`（区块内容）。

---

### 9.10 组件目录结构（当前落地）

```
admin/components/
├── elder-search/ElderSearch.uvue           # ✅ 已实现
├── staff-search/StaffSearch.uvue           # ✅ 已实现
├── date-time-picker/DateTimePicker.uvue    # ✅ 已实现
├── image-uploader/ImageUploader.uvue       # ✅ 已实现
├── status-tag/StatusTag.uvue               # ✅ 已实现
├── dishes-picker/DishesPicker.uvue         # ✅ 已实现
├── contact-quick-fill/ContactQuickFill.uvue# ✅ 已实现
├── wizard-form/WizardForm.uvue             # ⚠️ 4 个新增向导已引用，组件文件待补齐
└── README.md                               # 组件契约与实现状态基线
（规划：list-page/、section-card/，尚未建立）
```

> 组件命名遵循 uni-app-x 规范（多词小驼峰，目录与组件名一致）；页面中 `easycom` 自动引入 `components/` 下的同名组件。