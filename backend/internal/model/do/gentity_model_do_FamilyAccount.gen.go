// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tblfamilyaccount"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

var (
	familyaccountPool = pool.New[*FamilyAccount](func() any {
		_obj := &FamilyAccount{}
		return _obj
	})
)

func NewFamilyAccount() *FamilyAccount {
	_obj := familyaccountPool.Get()
	return _obj
}

// MarshalJSON
func (p *FamilyAccount) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(7 * 50)
	write.WriteRaw("id", types.Marshal(p.Id))
	write.WriteRaw("phone", types.Marshal(p.Phone))
	write.WriteRaw("pass", types.Marshal(p.Pass))
	write.WriteRaw("openid", types.Marshal(p.Openid))
	write.WriteRaw("create_time", types.Marshal(p.CreateTime))
	write.WriteRaw("update_time", types.Marshal(p.UpdateTime))
	write.WriteRaw("del_flag", types.Marshal(p.DelFlag))
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *FamilyAccount) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "phone":
			p.Phone = types.String(value.Str)
		case "pass":
			p.Pass = types.String(value.Str)
		case "openid":
			p.Openid = types.String(value.Str)
		case "create_time":
			p.CreateTime = types.Time{Time: value.Time()}
		case "update_time":
			p.UpdateTime = types.Time{Time: value.Time()}
		case "del_flag":
			p.DelFlag = types.Int8(value.Int())
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *FamilyAccount) Free() {
	if p == nil {
		return
	}

	familyaccountPool.Put(p)
}

// Reset
func (p *FamilyAccount) Reset() {
	p.Id = 0
	p.Phone = ""
	p.Pass = ""
	p.Openid = ""
	p.CreateTime = types.Time{}
	p.UpdateTime = types.Time{}
	p.DelFlag = 0

}

func (p *FamilyAccount) TableName() string {
	return "family_account"
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var familyaccountFieldToPtrFunc = map[string]func(*FamilyAccount) any{
	tblfamilyaccount.Id.Name:         func(p *FamilyAccount) any { return &p.Id },
	tblfamilyaccount.Phone.Name:      func(p *FamilyAccount) any { return &p.Phone },
	tblfamilyaccount.Pass.Name:       func(p *FamilyAccount) any { return &p.Pass },
	tblfamilyaccount.Openid.Name:     func(p *FamilyAccount) any { return &p.Openid },
	tblfamilyaccount.CreateTime.Name: func(p *FamilyAccount) any { return &p.CreateTime },
	tblfamilyaccount.UpdateTime.Name: func(p *FamilyAccount) any { return &p.UpdateTime },
	tblfamilyaccount.DelFlag.Name:    func(p *FamilyAccount) any { return &p.DelFlag },
}

// fieldPtr 根据字段参数，返回对应的指针获取函数列表（与具体实例无关，可缓存复用）
func (p *FamilyAccount) fieldPtr(args ...dialect.Field) []func(*FamilyAccount) any {
	if len(args) == 0 {
		args = tblfamilyaccount.ReadableFields
	}
	fs := make([]func(*FamilyAccount) any, 0, len(args))
	for _, col := range args {
		if f, ok := familyaccountFieldToPtrFunc[col.Name]; ok {
			fs = append(fs, f)
		}
	}
	return fs
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *FamilyAccount) AssignPtr(args ...dialect.Field) []any {
	fs := p.fieldPtr(args...)
	_vals := make([]any, len(fs))
	for i, f := range fs {
		_vals[i] = f(p)
	}
	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *FamilyAccount) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if f, ok := familyaccountFieldToPtrFunc[col]; ok {
			_vals = append(_vals, f(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *FamilyAccount) Slice(rows *sql.Rows, args ...dialect.Field) ([]*FamilyAccount, bool, error) {
	defer rows.Close()
	family_accounts := make([]*FamilyAccount, 0)

	// 只获取一次：字段 -> ptrFunc 的有序列表（与实例无关）
	fs := p.fieldPtr(args...)

	// 复用的扫描目标切片，循环外分配一次
	_vals := make([]any, len(fs))

	for rows.Next() {
		_p := NewFamilyAccount()
		// 每行只做"指针绑定到新实例"，
		for i, f := range fs {
			_vals[i] = f(_p)
		}

		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		family_accounts = append(family_accounts, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return family_accounts, len(family_accounts) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *FamilyAccount) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tblfamilyaccount.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var familyaccountFieldToValueFunc = map[dialect.Field]func(*FamilyAccount) (any, bool){
	tblfamilyaccount.Id: func(p *FamilyAccount) (any, bool) {
		return p.Id, p.Id == 0
	},
	tblfamilyaccount.Phone: func(p *FamilyAccount) (any, bool) {
		return p.Phone, p.Phone == ""
	},
	tblfamilyaccount.Pass: func(p *FamilyAccount) (any, bool) {
		return p.Pass, p.Pass == ""
	},
	tblfamilyaccount.Openid: func(p *FamilyAccount) (any, bool) {
		return p.Openid, p.Openid == ""
	},
	tblfamilyaccount.CreateTime: func(p *FamilyAccount) (any, bool) {
		return p.CreateTime, p.CreateTime.IsZero()
	},
	tblfamilyaccount.UpdateTime: func(p *FamilyAccount) (any, bool) {
		return p.UpdateTime, p.UpdateTime.IsZero()
	},
	tblfamilyaccount.DelFlag: func(p *FamilyAccount) (any, bool) {
		return p.DelFlag, p.DelFlag == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *FamilyAccount) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tblfamilyaccount.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if f, has := familyaccountFieldToValueFunc[arg]; has {
			value, isZero := f(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *FamilyAccount) AssignKeys() (dialect.Field, any) {
	return tblfamilyaccount.PrimaryKey, p.Id
}

func (p *FamilyAccount) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
