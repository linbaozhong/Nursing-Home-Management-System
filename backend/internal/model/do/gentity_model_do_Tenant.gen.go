// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tbltenant"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

var (
	tenantPool = pool.New[*Tenant](func() any {
		_obj := &Tenant{}
		return _obj
	})
)

func NewTenant() *Tenant {
	_obj := tenantPool.Get()
	return _obj
}

// MarshalJSON
func (p *Tenant) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(13 * 50)
	if p.ContactName != "" {
		write.WriteRaw("contact_name", types.Marshal(p.ContactName))
	}
	if p.ContactPhone != "" {
		write.WriteRaw("contact_phone", types.Marshal(p.ContactPhone))
	}
	if p.Logo != "" {
		write.WriteRaw("logo", types.Marshal(p.Logo))
	}
	if p.Name != "" {
		write.WriteRaw("name", types.Marshal(p.Name))
	}
	if p.Plan != "" {
		write.WriteRaw("plan", types.Marshal(p.Plan))
	}
	if p.CreateId != 0 {
		write.WriteRaw("create_id", types.Marshal(p.CreateId))
	}
	if !p.CreateTime.IsZero() {
		write.WriteRaw("create_time", types.Marshal(p.CreateTime))
	}
	if !p.ExpireTime.IsZero() {
		write.WriteRaw("expire_time", types.Marshal(p.ExpireTime))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.UpdateId != 0 {
		write.WriteRaw("update_id", types.Marshal(p.UpdateId))
	}
	if !p.UpdateTime.IsZero() {
		write.WriteRaw("update_time", types.Marshal(p.UpdateTime))
	}
	if p.State != 0 {
		write.WriteRaw("state", types.Marshal(p.State))
	}
	if p.Status != 0 {
		write.WriteRaw("status", types.Marshal(p.Status))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *Tenant) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "contact_name":
			p.ContactName = types.String(value.Str)
		case "contact_phone":
			p.ContactPhone = types.String(value.Str)
		case "logo":
			p.Logo = types.String(value.Str)
		case "name":
			p.Name = types.String(value.Str)
		case "plan":
			p.Plan = types.String(value.Str)
		case "create_id":
			p.CreateId = types.BigInt(value.Uint())
		case "create_time":
			p.CreateTime = types.Time{Time: value.Time()}
		case "expire_time":
			p.ExpireTime = types.Time{Time: value.Time()}
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "update_id":
			p.UpdateId = types.BigInt(value.Uint())
		case "update_time":
			p.UpdateTime = types.Time{Time: value.Time()}
		case "state":
			p.State = types.Int8(value.Int())
		case "status":
			p.Status = types.Uint8(value.Uint())
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *Tenant) Free() {
	if p == nil {
		return
	}

	tenantPool.Put(p)
}

// Reset
func (p *Tenant) Reset() {
	p.ContactName = ""
	p.ContactPhone = ""
	p.Logo = ""
	p.Name = ""
	p.Plan = ""
	p.CreateId = 0
	p.CreateTime = types.Time{}
	p.ExpireTime = types.Time{}
	p.Id = 0
	p.UpdateId = 0
	p.UpdateTime = types.Time{}
	p.State = 0
	p.Status = 0

}

func (p *Tenant) TableName() string {
	return "tenant"
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var tenantFieldToPtrFunc = map[string]func(*Tenant) any{
	tbltenant.ContactName.Name:  func(p *Tenant) any { return &p.ContactName },
	tbltenant.ContactPhone.Name: func(p *Tenant) any { return &p.ContactPhone },
	tbltenant.Logo.Name:         func(p *Tenant) any { return &p.Logo },
	tbltenant.Name.Name:         func(p *Tenant) any { return &p.Name },
	tbltenant.Plan.Name:         func(p *Tenant) any { return &p.Plan },
	tbltenant.CreateId.Name:     func(p *Tenant) any { return &p.CreateId },
	tbltenant.CreateTime.Name:   func(p *Tenant) any { return &p.CreateTime },
	tbltenant.ExpireTime.Name:   func(p *Tenant) any { return &p.ExpireTime },
	tbltenant.Id.Name:           func(p *Tenant) any { return &p.Id },
	tbltenant.UpdateId.Name:     func(p *Tenant) any { return &p.UpdateId },
	tbltenant.UpdateTime.Name:   func(p *Tenant) any { return &p.UpdateTime },
	tbltenant.State.Name:        func(p *Tenant) any { return &p.State },
	tbltenant.Status.Name:       func(p *Tenant) any { return &p.Status },
}

// fieldPtr 根据字段参数，返回对应的指针获取函数列表（与具体实例无关，可缓存复用）
func (p *Tenant) fieldPtr(args ...dialect.Field) []func(*Tenant) any {
	if len(args) == 0 {
		args = tbltenant.ReadableFields
	}
	fs := make([]func(*Tenant) any, 0, len(args))
	for _, col := range args {
		if f, ok := tenantFieldToPtrFunc[col.Name]; ok {
			fs = append(fs, f)
		}
	}
	return fs
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *Tenant) AssignPtr(args ...dialect.Field) []any {
	fs := p.fieldPtr(args...)
	_vals := make([]any, len(fs))
	for i, f := range fs {
		_vals[i] = f(p)
	}
	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *Tenant) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if f, ok := tenantFieldToPtrFunc[col]; ok {
			_vals = append(_vals, f(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *Tenant) Slice(rows *sql.Rows, args ...dialect.Field) ([]*Tenant, bool, error) {
	defer rows.Close()
	tenants := make([]*Tenant, 0)

	// 只获取一次：字段 -> ptrFunc 的有序列表（与实例无关）
	fs := p.fieldPtr(args...)

	// 复用的扫描目标切片，循环外分配一次
	_vals := make([]any, len(fs))

	for rows.Next() {
		_p := NewTenant()
		// 每行只做"指针绑定到新实例"，
		for i, f := range fs {
			_vals[i] = f(_p)
		}

		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		tenants = append(tenants, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return tenants, len(tenants) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Tenant) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tbltenant.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var tenantFieldToValueFunc = map[dialect.Field]func(*Tenant) (any, bool){
	tbltenant.ContactName: func(p *Tenant) (any, bool) {
		return p.ContactName, p.ContactName == ""
	},
	tbltenant.ContactPhone: func(p *Tenant) (any, bool) {
		return p.ContactPhone, p.ContactPhone == ""
	},
	tbltenant.Logo: func(p *Tenant) (any, bool) {
		return p.Logo, p.Logo == ""
	},
	tbltenant.Name: func(p *Tenant) (any, bool) {
		return p.Name, p.Name == ""
	},
	tbltenant.Plan: func(p *Tenant) (any, bool) {
		return p.Plan, p.Plan == ""
	},
	tbltenant.CreateId: func(p *Tenant) (any, bool) {
		return p.CreateId, p.CreateId == 0
	},
	tbltenant.CreateTime: func(p *Tenant) (any, bool) {
		return p.CreateTime, p.CreateTime.IsZero()
	},
	tbltenant.ExpireTime: func(p *Tenant) (any, bool) {
		return p.ExpireTime, p.ExpireTime.IsZero()
	},
	tbltenant.Id: func(p *Tenant) (any, bool) {
		return p.Id, p.Id == 0
	},
	tbltenant.UpdateId: func(p *Tenant) (any, bool) {
		return p.UpdateId, p.UpdateId == 0
	},
	tbltenant.UpdateTime: func(p *Tenant) (any, bool) {
		return p.UpdateTime, p.UpdateTime.IsZero()
	},
	tbltenant.State: func(p *Tenant) (any, bool) {
		return p.State, p.State == 0
	},
	tbltenant.Status: func(p *Tenant) (any, bool) {
		return p.Status, p.Status == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Tenant) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tbltenant.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if f, has := tenantFieldToValueFunc[arg]; has {
			value, isZero := f(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *Tenant) AssignKeys() (dialect.Field, any) {
	return tbltenant.PrimaryKey, p.Id
}

func (p *Tenant) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
