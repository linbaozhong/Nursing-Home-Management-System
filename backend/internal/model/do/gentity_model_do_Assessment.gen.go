// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tblassessment"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

var (
	assessmentPool = pool.New[*Assessment](func() any {
		_obj := &Assessment{}
		return _obj
	})
)

func NewAssessment() *Assessment {
	_obj := assessmentPool.Get()
	return _obj
}

// MarshalJSON
func (p *Assessment) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(11 * 50)
	if p.ResultLevel != "" {
		write.WriteRaw("result_level", types.Marshal(p.ResultLevel))
	}
	if p.ScaleData != "" {
		write.WriteRaw("scale_data", types.Marshal(p.ScaleData))
	}
	if !p.AssessmentDate.IsZero() {
		write.WriteRaw("assessment_date", types.Marshal(p.AssessmentDate))
	}
	if !p.CreatedAt.IsZero() {
		write.WriteRaw("created_at", types.Marshal(p.CreatedAt))
	}
	if !p.DeletedAt.IsZero() {
		write.WriteRaw("deleted_at", types.Marshal(p.DeletedAt))
	}
	if p.ElderId != 0 {
		write.WriteRaw("elder_id", types.Marshal(p.ElderId))
	}
	if p.EvaluatorId != 0 {
		write.WriteRaw("evaluator_id", types.Marshal(p.EvaluatorId))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.TenantId != 0 {
		write.WriteRaw("tenant_id", types.Marshal(p.TenantId))
	}
	if !p.UpdatedAt.IsZero() {
		write.WriteRaw("updated_at", types.Marshal(p.UpdatedAt))
	}
	if p.AssessmentType != 0 {
		write.WriteRaw("assessment_type", types.Marshal(p.AssessmentType))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *Assessment) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "result_level":
			p.ResultLevel = types.String(value.Str)
		case "scale_data":
			p.ScaleData = types.String(value.Str)
		case "assessment_date":
			p.AssessmentDate = types.Time{Time: value.Time()}
		case "created_at":
			p.CreatedAt = types.Time{Time: value.Time()}
		case "deleted_at":
			p.DeletedAt = types.Time{Time: value.Time()}
		case "elder_id":
			p.ElderId = types.BigInt(value.Uint())
		case "evaluator_id":
			p.EvaluatorId = types.BigInt(value.Uint())
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "tenant_id":
			p.TenantId = types.BigInt(value.Uint())
		case "updated_at":
			p.UpdatedAt = types.Time{Time: value.Time()}
		case "assessment_type":
			p.AssessmentType = types.Int8(value.Int())
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *Assessment) Free() {
	if p == nil {
		return
	}

	assessmentPool.Put(p)
}

// Reset
func (p *Assessment) Reset() {
	p.ResultLevel = ""
	p.ScaleData = ""
	p.AssessmentDate = types.Time{}
	p.CreatedAt = types.Time{}
	p.DeletedAt = types.Time{}
	p.ElderId = 0
	p.EvaluatorId = 0
	p.Id = 0
	p.TenantId = 0
	p.UpdatedAt = types.Time{}
	p.AssessmentType = 0

}

func (p *Assessment) TableName() string {
	return "assessment"
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var assessmentFieldToPtrFunc = map[string]func(*Assessment) any{
	tblassessment.ResultLevel.Name:    func(p *Assessment) any { return &p.ResultLevel },
	tblassessment.ScaleData.Name:      func(p *Assessment) any { return &p.ScaleData },
	tblassessment.AssessmentDate.Name: func(p *Assessment) any { return &p.AssessmentDate },
	tblassessment.CreatedAt.Name:      func(p *Assessment) any { return &p.CreatedAt },
	tblassessment.DeletedAt.Name:      func(p *Assessment) any { return &p.DeletedAt },
	tblassessment.ElderId.Name:        func(p *Assessment) any { return &p.ElderId },
	tblassessment.EvaluatorId.Name:    func(p *Assessment) any { return &p.EvaluatorId },
	tblassessment.Id.Name:             func(p *Assessment) any { return &p.Id },
	tblassessment.TenantId.Name:       func(p *Assessment) any { return &p.TenantId },
	tblassessment.UpdatedAt.Name:      func(p *Assessment) any { return &p.UpdatedAt },
	tblassessment.AssessmentType.Name: func(p *Assessment) any { return &p.AssessmentType },
}

// fieldPtr 根据字段参数，返回对应的指针获取函数列表（与具体实例无关，可缓存复用）
func (p *Assessment) fieldPtr(args ...dialect.Field) []func(*Assessment) any {
	if len(args) == 0 {
		args = tblassessment.ReadableFields
	}
	fs := make([]func(*Assessment) any, 0, len(args))
	for _, col := range args {
		if f, ok := assessmentFieldToPtrFunc[col.Name]; ok {
			fs = append(fs, f)
		}
	}
	return fs
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *Assessment) AssignPtr(args ...dialect.Field) []any {
	fs := p.fieldPtr(args...)
	_vals := make([]any, len(fs))
	for i, f := range fs {
		_vals[i] = f(p)
	}
	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *Assessment) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if f, ok := assessmentFieldToPtrFunc[col]; ok {
			_vals = append(_vals, f(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *Assessment) Slice(rows *sql.Rows, args ...dialect.Field) ([]*Assessment, bool, error) {
	defer rows.Close()
	assessments := make([]*Assessment, 0)

	// 只获取一次：字段 -> ptrFunc 的有序列表（与实例无关）
	fs := p.fieldPtr(args...)

	// 复用的扫描目标切片，循环外分配一次
	_vals := make([]any, len(fs))

	for rows.Next() {
		_p := NewAssessment()
		// 每行只做"指针绑定到新实例"，
		for i, f := range fs {
			_vals[i] = f(_p)
		}

		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		assessments = append(assessments, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return assessments, len(assessments) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Assessment) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tblassessment.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var assessmentFieldToValueFunc = map[dialect.Field]func(*Assessment) (any, bool){
	tblassessment.ResultLevel: func(p *Assessment) (any, bool) {
		return p.ResultLevel, p.ResultLevel == ""
	},
	tblassessment.ScaleData: func(p *Assessment) (any, bool) {
		return p.ScaleData, p.ScaleData == ""
	},
	tblassessment.AssessmentDate: func(p *Assessment) (any, bool) {
		return p.AssessmentDate, p.AssessmentDate.IsZero()
	},
	tblassessment.CreatedAt: func(p *Assessment) (any, bool) {
		return p.CreatedAt, p.CreatedAt.IsZero()
	},
	tblassessment.DeletedAt: func(p *Assessment) (any, bool) {
		return p.DeletedAt, p.DeletedAt.IsZero()
	},
	tblassessment.ElderId: func(p *Assessment) (any, bool) {
		return p.ElderId, p.ElderId == 0
	},
	tblassessment.EvaluatorId: func(p *Assessment) (any, bool) {
		return p.EvaluatorId, p.EvaluatorId == 0
	},
	tblassessment.Id: func(p *Assessment) (any, bool) {
		return p.Id, p.Id == 0
	},
	tblassessment.TenantId: func(p *Assessment) (any, bool) {
		return p.TenantId, p.TenantId == 0
	},
	tblassessment.UpdatedAt: func(p *Assessment) (any, bool) {
		return p.UpdatedAt, p.UpdatedAt.IsZero()
	},
	tblassessment.AssessmentType: func(p *Assessment) (any, bool) {
		return p.AssessmentType, p.AssessmentType == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Assessment) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tblassessment.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if f, has := assessmentFieldToValueFunc[arg]; has {
			value, isZero := f(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *Assessment) AssignKeys() (dialect.Field, any) {
	return tblassessment.PrimaryKey, p.Id
}

func (p *Assessment) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
