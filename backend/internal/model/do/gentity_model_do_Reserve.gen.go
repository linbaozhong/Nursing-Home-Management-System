// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tblreserve"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

var (
	reservePool = pool.New[*Reserve](func() any {
		_obj := &Reserve{}
		return _obj
	})
)

func NewReserve() *Reserve {
	_obj := reservePool.Get()
	return _obj
}

// MarshalJSON
func (p *Reserve) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(13 * 50)
	if p.Name != "" {
		write.WriteRaw("name", types.Marshal(p.Name))
	}
	if p.Phone != "" {
		write.WriteRaw("phone", types.Marshal(p.Phone))
	}
	if p.CreateId != 0 {
		write.WriteRaw("create_id", types.Marshal(p.CreateId))
	}
	if !p.CreateTime.IsZero() {
		write.WriteRaw("create_time", types.Marshal(p.CreateTime))
	}
	if p.Deposit != 0 {
		write.WriteRaw("deposit", types.Marshal(p.Deposit))
	}
	if !p.DueDate.IsZero() {
		write.WriteRaw("due_date", types.Marshal(p.DueDate))
	}
	if p.ElderId != 0 {
		write.WriteRaw("elder_id", types.Marshal(p.ElderId))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.StaffId != 0 {
		write.WriteRaw("staff_id", types.Marshal(p.StaffId))
	}
	if p.TenantId != 0 {
		write.WriteRaw("tenant_id", types.Marshal(p.TenantId))
	}
	if p.UpdateId != 0 {
		write.WriteRaw("update_id", types.Marshal(p.UpdateId))
	}
	if !p.UpdateTime.IsZero() {
		write.WriteRaw("update_time", types.Marshal(p.UpdateTime))
	}
	if p.Status != 0 {
		write.WriteRaw("status", types.Marshal(p.Status))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *Reserve) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "name":
			p.Name = types.String(value.Str)
		case "phone":
			p.Phone = types.String(value.Str)
		case "create_id":
			p.CreateId = types.BigInt(value.Uint())
		case "create_time":
			p.CreateTime = types.Time{Time: value.Time()}
		case "deposit":
			e = types.Unmarshal(value, &p.Deposit)
		case "due_date":
			p.DueDate = types.Time{Time: value.Time()}
		case "elder_id":
			p.ElderId = types.BigInt(value.Uint())
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "staff_id":
			p.StaffId = types.BigInt(value.Uint())
		case "tenant_id":
			p.TenantId = types.BigInt(value.Uint())
		case "update_id":
			p.UpdateId = types.BigInt(value.Uint())
		case "update_time":
			p.UpdateTime = types.Time{Time: value.Time()}
		case "status":
			p.Status = types.Int8(value.Int())
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *Reserve) Free() {
	if p == nil {
		return
	}

	reservePool.Put(p)
}

// Reset
func (p *Reserve) Reset() {
	p.Name = ""
	p.Phone = ""
	p.CreateId = 0
	p.CreateTime = types.Time{}
	p.Deposit = 0
	p.DueDate = types.Time{}
	p.ElderId = 0
	p.Id = 0
	p.StaffId = 0
	p.TenantId = 0
	p.UpdateId = 0
	p.UpdateTime = types.Time{}
	p.Status = 0

}

func (p *Reserve) TableName() string {
	return "reserve"
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var reserveFieldToPtrFunc = map[string]func(*Reserve) any{
	tblreserve.Name.Name:       func(p *Reserve) any { return &p.Name },
	tblreserve.Phone.Name:      func(p *Reserve) any { return &p.Phone },
	tblreserve.CreateId.Name:   func(p *Reserve) any { return &p.CreateId },
	tblreserve.CreateTime.Name: func(p *Reserve) any { return &p.CreateTime },
	tblreserve.Deposit.Name:    func(p *Reserve) any { return &p.Deposit },
	tblreserve.DueDate.Name:    func(p *Reserve) any { return &p.DueDate },
	tblreserve.ElderId.Name:    func(p *Reserve) any { return &p.ElderId },
	tblreserve.Id.Name:         func(p *Reserve) any { return &p.Id },
	tblreserve.StaffId.Name:    func(p *Reserve) any { return &p.StaffId },
	tblreserve.TenantId.Name:   func(p *Reserve) any { return &p.TenantId },
	tblreserve.UpdateId.Name:   func(p *Reserve) any { return &p.UpdateId },
	tblreserve.UpdateTime.Name: func(p *Reserve) any { return &p.UpdateTime },
	tblreserve.Status.Name:     func(p *Reserve) any { return &p.Status },
}

// fieldPtr 根据字段参数，返回对应的指针获取函数列表（与具体实例无关，可缓存复用）
func (p *Reserve) fieldPtr(args ...dialect.Field) []func(*Reserve) any {
	if len(args) == 0 {
		args = tblreserve.ReadableFields
	}
	fs := make([]func(*Reserve) any, 0, len(args))
	for _, col := range args {
		if f, ok := reserveFieldToPtrFunc[col.Name]; ok {
			fs = append(fs, f)
		}
	}
	return fs
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *Reserve) AssignPtr(args ...dialect.Field) []any {
	fs := p.fieldPtr(args...)
	_vals := make([]any, len(fs))
	for i, f := range fs {
		_vals[i] = f(p)
	}
	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *Reserve) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if f, ok := reserveFieldToPtrFunc[col]; ok {
			_vals = append(_vals, f(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *Reserve) Slice(rows *sql.Rows, args ...dialect.Field) ([]*Reserve, bool, error) {
	defer rows.Close()
	reserves := make([]*Reserve, 0)

	// 只获取一次：字段 -> ptrFunc 的有序列表（与实例无关）
	fs := p.fieldPtr(args...)

	// 复用的扫描目标切片，循环外分配一次
	_vals := make([]any, len(fs))

	for rows.Next() {
		_p := NewReserve()
		// 每行只做"指针绑定到新实例"，
		for i, f := range fs {
			_vals[i] = f(_p)
		}

		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		reserves = append(reserves, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return reserves, len(reserves) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Reserve) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tblreserve.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var reserveFieldToValueFunc = map[dialect.Field]func(*Reserve) (any, bool){
	tblreserve.Name: func(p *Reserve) (any, bool) {
		return p.Name, p.Name == ""
	},
	tblreserve.Phone: func(p *Reserve) (any, bool) {
		return p.Phone, p.Phone == ""
	},
	tblreserve.CreateId: func(p *Reserve) (any, bool) {
		return p.CreateId, p.CreateId == 0
	},
	tblreserve.CreateTime: func(p *Reserve) (any, bool) {
		return p.CreateTime, p.CreateTime.IsZero()
	},
	tblreserve.Deposit: func(p *Reserve) (any, bool) {
		return p.Deposit, p.Deposit == 0
	},
	tblreserve.DueDate: func(p *Reserve) (any, bool) {
		return p.DueDate, p.DueDate.IsZero()
	},
	tblreserve.ElderId: func(p *Reserve) (any, bool) {
		return p.ElderId, p.ElderId == 0
	},
	tblreserve.Id: func(p *Reserve) (any, bool) {
		return p.Id, p.Id == 0
	},
	tblreserve.StaffId: func(p *Reserve) (any, bool) {
		return p.StaffId, p.StaffId == 0
	},
	tblreserve.TenantId: func(p *Reserve) (any, bool) {
		return p.TenantId, p.TenantId == 0
	},
	tblreserve.UpdateId: func(p *Reserve) (any, bool) {
		return p.UpdateId, p.UpdateId == 0
	},
	tblreserve.UpdateTime: func(p *Reserve) (any, bool) {
		return p.UpdateTime, p.UpdateTime.IsZero()
	},
	tblreserve.Status: func(p *Reserve) (any, bool) {
		return p.Status, p.Status == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *Reserve) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tblreserve.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if f, has := reserveFieldToValueFunc[arg]; has {
			value, isZero := f(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *Reserve) AssignKeys() (dialect.Field, any) {
	return tblreserve.PrimaryKey, p.Id
}

func (p *Reserve) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
