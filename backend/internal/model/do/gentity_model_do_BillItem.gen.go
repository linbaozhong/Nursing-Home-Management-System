// Code generated by github.com/linbaozhong/gentity. DO NOT EDIT.

package do

import (
	"api/internal/model/define/table/tblbillitem"
	"database/sql"
	"errors"
	"github.com/linbaozhong/gentity/pkg/ace/dialect"
	"github.com/linbaozhong/gentity/pkg/ace/pool"
	"github.com/linbaozhong/gentity/pkg/gjson"
	"github.com/linbaozhong/gentity/pkg/log"
	"github.com/linbaozhong/gentity/pkg/types"
)

var (
	billitemPool = pool.New[*BillItem](func() any {
		_obj := &BillItem{}
		return _obj
	})
)

func NewBillItem() *BillItem {
	_obj := billitemPool.Get()
	return _obj
}

// MarshalJSON
func (p *BillItem) MarshalJSON() ([]byte, error) {
	write := types.NewJsonWriter(8 * 50)
	if p.Description != "" {
		write.WriteRaw("description", types.Marshal(p.Description))
	}
	if p.Amount != 0 {
		write.WriteRaw("amount", types.Marshal(p.Amount))
	}
	if p.BillId != 0 {
		write.WriteRaw("bill_id", types.Marshal(p.BillId))
	}
	if !p.CreatedTime.IsZero() {
		write.WriteRaw("created_time", types.Marshal(p.CreatedTime))
	}
	if p.FeeItemId != 0 {
		write.WriteRaw("fee_item_id", types.Marshal(p.FeeItemId))
	}
	if p.Id != 0 {
		write.WriteRaw("id", types.Marshal(p.Id))
	}
	if p.Quantity != 0.0 {
		write.WriteRaw("quantity", types.Marshal(p.Quantity))
	}
	if p.UnitPrice != 0 {
		write.WriteRaw("unit_price", types.Marshal(p.UnitPrice))
	}
	return write.Bytes(), nil
}

// UnmarshalJSON
func (p *BillItem) UnmarshalJSON(data []byte) error {

	if !gjson.ValidBytes(data) {
		return errors.New("invalid json")
	}
	_result := gjson.ParseBytes(data)
	_result.ForEach(func(key, value gjson.Result) bool {
		var e error
		switch key.Str {
		case "description":
			p.Description = types.String(value.Str)
		case "amount":
			e = types.Unmarshal(value, &p.Amount)
		case "bill_id":
			p.BillId = types.BigInt(value.Uint())
		case "created_time":
			p.CreatedTime = types.Time{Time: value.Time()}
		case "fee_item_id":
			p.FeeItemId = types.BigInt(value.Uint())
		case "id":
			p.Id = types.BigInt(value.Uint())
		case "quantity":
			p.Quantity = types.Float64(value.Float())
		case "unit_price":
			e = types.Unmarshal(value, &p.UnitPrice)
		}
		if e != nil {
			log.Error(e)
			return false
		}
		return true
	})
	return nil
}

// Free
func (p *BillItem) Free() {
	if p == nil {
		return
	}

	billitemPool.Put(p)
}

// Reset
func (p *BillItem) Reset() {
	p.Description = ""
	p.Amount = 0
	p.BillId = 0
	p.CreatedTime = types.Time{}
	p.FeeItemId = 0
	p.Id = 0
	p.Quantity = 0
	p.UnitPrice = 0

}

func (p *BillItem) TableName() string {
	return "bill_item"
}

// 定义一个映射表，将字段与对应的指针获取函数关联
var billitemFieldToPtrFunc = map[string]func(*BillItem) any{
	tblbillitem.Description.Name: func(p *BillItem) any { return &p.Description },
	tblbillitem.Amount.Name:      func(p *BillItem) any { return &p.Amount },
	tblbillitem.BillId.Name:      func(p *BillItem) any { return &p.BillId },
	tblbillitem.CreatedTime.Name: func(p *BillItem) any { return &p.CreatedTime },
	tblbillitem.FeeItemId.Name:   func(p *BillItem) any { return &p.FeeItemId },
	tblbillitem.Id.Name:          func(p *BillItem) any { return &p.Id },
	tblbillitem.Quantity.Name:    func(p *BillItem) any { return &p.Quantity },
	tblbillitem.UnitPrice.Name:   func(p *BillItem) any { return &p.UnitPrice },
}

// fieldPtr 根据字段参数，返回对应的指针获取函数列表（与具体实例无关，可缓存复用）
func (p *BillItem) fieldPtr(args ...dialect.Field) []func(*BillItem) any {
	if len(args) == 0 {
		args = tblbillitem.ReadableFields
	}
	fs := make([]func(*BillItem) any, 0, len(args))
	for _, col := range args {
		if f, ok := billitemFieldToPtrFunc[col.Name]; ok {
			fs = append(fs, f)
		}
	}
	return fs
}

// AssignPtr 根据传入的字段参数，返回对应字段的指针切片。
// 如果未传入任何字段参数，则默认使用 ReadableFields 中的字段。
// 参数 args 为可变参数，代表需要获取指针的字段。
// 返回值为一个包含对应字段指针的切片。
func (p *BillItem) AssignPtr(args ...dialect.Field) []any {
	fs := p.fieldPtr(args...)
	_vals := make([]any, len(fs))
	for i, f := range fs {
		_vals[i] = f(p)
	}
	return _vals
}

// AssignPtrByColumns 根据 SQL 实际返回的列名，按列顺序返回对应字段的指针切片。
// 列在映射表中找不到对应字段时，用一个占位指针跳过（保持列数/顺序与 rows 一致），避免 Scan 报错。
// 参数 cols 为 rows.Columns() 返回的列名切片。
func (p *BillItem) AssignPtrByColumns(cols ...string) []any {
	_vals := make([]any, 0, len(cols))
	for _, col := range cols {
		if f, ok := billitemFieldToPtrFunc[col]; ok {
			_vals = append(_vals, f(p))
			continue
		}
		// 列名在结构体中找不到对应字段：用忽略指针占位，保证列数对齐
		var ignore any
		_vals = append(_vals, &ignore)
	}
	return _vals
}

func (p *BillItem) Slice(rows *sql.Rows, args ...dialect.Field) ([]*BillItem, bool, error) {
	defer rows.Close()
	bill_items := make([]*BillItem, 0)

	// 只获取一次：字段 -> ptrFunc 的有序列表（与实例无关）
	fs := p.fieldPtr(args...)

	// 复用的扫描目标切片，循环外分配一次
	_vals := make([]any, len(fs))

	for rows.Next() {
		_p := NewBillItem()
		// 每行只做"指针绑定到新实例"，
		for i, f := range fs {
			_vals[i] = f(_p)
		}

		e := rows.Scan(_vals...)
		if e != nil {
			log.Error(e)
			return nil, false, e
		}
		bill_items = append(bill_items, _p)
	}
	if e := rows.Err(); e != nil {
		log.Error(e)
		return nil, false, e
	}
	return bill_items, len(bill_items) > 0, nil
}

// RawAssignValues 向数据库写入数据前，为表列赋值。多用于批量插入和更新
// 如果 args 为空，则赋值所有可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *BillItem) RawAssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	if len(args) == 0 {
		args = tblbillitem.WritableFields
	}
	return p.AssignValues(d, args...)
}

// 定义字段到值检查和获取函数的映射
var billitemFieldToValueFunc = map[dialect.Field]func(*BillItem) (any, bool){
	tblbillitem.Description: func(p *BillItem) (any, bool) {
		return p.Description, p.Description == ""
	},
	tblbillitem.Amount: func(p *BillItem) (any, bool) {
		return p.Amount, p.Amount == 0
	},
	tblbillitem.BillId: func(p *BillItem) (any, bool) {
		return p.BillId, p.BillId == 0
	},
	tblbillitem.CreatedTime: func(p *BillItem) (any, bool) {
		return p.CreatedTime, p.CreatedTime.IsZero()
	},
	tblbillitem.FeeItemId: func(p *BillItem) (any, bool) {
		return p.FeeItemId, p.FeeItemId == 0
	},
	tblbillitem.Id: func(p *BillItem) (any, bool) {
		return p.Id, p.Id == 0
	},
	tblbillitem.Quantity: func(p *BillItem) (any, bool) {
		return p.Quantity, p.Quantity == 0.0
	},
	tblbillitem.UnitPrice: func(p *BillItem) (any, bool) {
		return p.UnitPrice, p.UnitPrice == 0
	},
}

// AssignValues 向数据库写入数据前，为表列赋值。
// 如果 args 为空，则将非零值赋与可写字段
// 如果 args 不为空，则只赋值 args 中的字段
func (p *BillItem) AssignValues(d dialect.Dialect, args ...dialect.Field) ([]string, []any) {
	// 未传参时使用全部可写字段，并跳过零值
	skipZero := len(args) == 0
	if skipZero {
		args = tblbillitem.WritableFields
	}

	cols := make([]string, 0, len(args))
	vals := make([]any, 0, len(args))

	for _, arg := range args {
		if f, has := billitemFieldToValueFunc[arg]; has {
			value, isZero := f(p)
			// 显式指定字段时全量包含；默认模式跳过零值字段
			if skipZero && isZero {
				continue
			}
			cols = append(cols, arg.Quote(d))
			vals = append(vals, value)
		}
	}
	return cols, vals
}

func (p *BillItem) AssignKeys() (dialect.Field, any) {
	return tblbillitem.PrimaryKey, p.Id
}

func (p *BillItem) AssignPrimaryKeyValues(result sql.Result) error {
	return nil
}
